import {
    IStoreUser,
} from './UserReducer';



/*
export function getAuthenticaticatedAccount(store: IStoreUser): IAccount | undefined {
    return store.user && store.user.account;
};

export function getLastActivityTime(store: IStoreUser): number | undefined {
    return store.user && store.user.lastActivityTime;
};
*/